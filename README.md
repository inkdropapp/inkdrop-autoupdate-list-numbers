# autoupdate-list-numbers plugin for Inkdrop

It automatically increments remaining Markdown list numbers when a new item is added to the middle of a list.

## Install

```sh
ipm install autoupdate-list-numbers
```

